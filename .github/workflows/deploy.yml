name: Deploy to Aliyun

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          echo "🚀 GitHub Actions connected! Starting deployment..."
          # 注意：下面这个路径要改成你服务器上 docker-compose.yml 所在的真实路径！
          cd /root/lobe-chat
          
          # 给予脚本执行权限（以防万一）
          chmod +x deploy.sh
          
          # 执行部署脚本
          ./deploy.sh
